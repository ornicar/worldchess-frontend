import { async, ComponentFixture, TestBed } from '@angular/core/testing';
import { GameLanguagesComponent } from './game-languages.component';

describe('GameLanguagesComponent', () => {
    let component: GameLanguagesComponent;
    let fixture: ComponentFixture<GameLanguagesComponent>;

    beforeEach(async(() => {
        TestBed.configureTestingModule({
            declarations: [ GameLanguagesComponent ]
        })
        .compileComponents();
    }));
});

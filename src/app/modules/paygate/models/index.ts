export interface ISpecialPlanProduct {
  name: string;
  amount: number;
  currency: string;
  old_price: number;
  interval: number;
  interval_count: number;
  stripe_id: string;
  status: number;
  created: string;
}
